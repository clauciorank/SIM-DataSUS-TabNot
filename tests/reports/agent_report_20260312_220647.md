# Relatório de testes do agente

Data: 2026-03-12T22:06:47.654315

Passaram: 30 | Falharam: 0 | Total: 30

---

## [OK] lugar_curitiba (lugar)

**Pergunta:** Quantos óbitos em Curitiba em 2023?

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE municipio_residencia = 'Curitiba' AND ano = 2023
```

---

## [OK] lugar_sao_bento_sul (lugar)

**Pergunta:** Óbitos em São Bento do Sul Santa Catarina nos últimos anos

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT * FROM v_obitos_analise WHERE municipio_residencia = 'São Bento do Sul' AND uf_residencia = 'SC' ORDER BY ano DESC LIMIT 10
```

---

## [OK] lugar_rio_janeiro (lugar)

**Pergunta:** Quantas mortes no Rio de Janeiro em 2022?

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT count(*) FROM v_obitos_analise WHERE municipio_residencia = 'Rio de Janeiro' AND ano = 2022
```

---

## [OK] lugar_estado_sc (lugar)

**Pergunta:** Total de óbitos em Santa Catarina em 2023

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE uf_residencia = 'SC' AND ano = 2023
```

---

## [OK] causa_dengue (causa)

**Pergunta:** Quantos óbitos por dengue no último ano?

**Resposta (trecho):** Foram registrados 67 óbitos por dengue no último ano....

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE (causa_basica IN ('A90') OR causa_basica IN ('A91')) AND ano = (SELECT MAX(ano) FROM v_obitos_analise)
```

---

## [OK] causa_covid (causa)

**Pergunta:** Mortes por COVID-19 em 2021

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE ano = 2021 AND (causa_basica LIKE 'U07.1%' OR causa_cid10_desc ILIKE '%COVID-19%')
```

---

## [OK] causa_pneumonia (causa)

**Pergunta:** Óbitos por pneumonia em 2023

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT count(*) FROM v_obitos_analise WHERE ano = 2023 AND (causa_basica IN ('A40.3') OR causa_basica IN ('B05.2') OR causa_basica IN ('B20.6') OR causa_basica IN ('B95.3') OR causa_basica IN ('B96.0', 'B96.1') OR causa_basica IN ('J10.0') OR causa_basica IN ('J11.0') OR causa_basica IN ('J12.8') OR causa_basica IN ('J13') OR causa_basica IN ('J15.0', 'J15.7') OR causa_basica IN ('J18.9'))
```

---

## [OK] causa_capitulo_neoplasias (causa)

**Pergunta:** Óbitos por capítulo de neoplasias em 2022

**Resposta (trecho):** Em 2022, ocorreram 6589 óbitos no capítulo de Neoplasias [tumores]....

**SQL (trecho):**
```sql
SELECT causa_cid10_capitulo_desc, COUNT(*) AS total_obitos FROM v_obitos_analise WHERE ano = 2022 AND causa_cid10_capitulo_desc IN ('Capítulo II - Neoplasias [tumores]') GROUP BY causa_cid10_capitulo_desc
```

---

## [OK] causa_doencas_infecciosas (causa)

**Pergunta:** Quantos óbitos por doenças infecciosas no Brasil?

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE causa_cid10_capitulo_desc = 'Doenças infecciosas e parasitárias'
```

---

## [OK] ano_2023 (ano)

**Pergunta:** Total de óbitos em 2023

**Resposta (trecho):** O total de óbitos em 2023 foi de 50.465....

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE ano = 2023
```

---

## [OK] ano_ultimos_3 (ano)

**Pergunta:** Quantos óbitos nos últimos 3 anos?

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE ano >= strftime('%Y', date('now', '-3 year'))
```

---

## [OK] agregacao_sexo (agregacao)

**Pergunta:** Óbitos por sexo em 2023

**Resposta (trecho):** Em 2023, ocorreram 21.113 óbitos do sexo feminino, 29.329 do sexo masculino e 23 óbitos de sexo ignorado....

**SQL (trecho):**
```sql
SELECT sexo_desc, COUNT(*) AS total_obitos FROM v_obitos_analise WHERE ano = 2023 GROUP BY sexo_desc
```

---

## [OK] agregacao_faixa_etaria (agregacao)

**Pergunta:** Distribuição de óbitos por faixa etária

**Resposta (trecho):** A distribuição de óbitos por faixa etária é a seguinte: 1-4 anos (5132), 10-14 anos (3275), 15-19 anos (13002), 20-29 anos (34594), 30-39 anos (34589), 40-49 anos (44395), 5-9 anos (2548), 50-59 anos (63886), 60-69 anos (88201), 70-79 anos (100334), 80+ anos (122913), < 1 ano (24646) e Ignorado (231...

**SQL (trecho):**
```sql
SELECT faixa_etaria, COUNT(*) AS total_obitos FROM v_obitos_analise GROUP BY faixa_etaria ORDER BY faixa_etaria
```

---

## [OK] agregacao_principais_causas (agregacao)

**Pergunta:** Quais as principais causas de óbito em 2022?

**Resposta (trecho):** As principais causas de óbito em 2022 foram: Infarto agudo do miocárdio não especificado (3162), Outras causas mal definidas e as não especificadas de mortalidade (2356), Infecção por coronavírus de localização não especificada (1725), Pneumonia não especificada (1676), Acidente vascular cerebral, n...

**SQL (trecho):**
```sql
SELECT causa_cid10_desc, COUNT(*) AS total_obitos FROM v_obitos_analise WHERE ano = 2022 GROUP BY causa_cid10_desc ORDER BY total_obitos DESC LIMIT 10
```

---

## [OK] agregacao_por_estado (agregacao)

**Pergunta:** Óbitos por estado em 2023

**Resposta (trecho):** Os óbitos por estado em 2023 foram: AL com 21962, AM com 20342, AC com 4187, AP com 3926 e None com 48....

**SQL (trecho):**
```sql
SELECT uf_residencia, COUNT(*) AS total_obitos FROM v_obitos_analise WHERE ano = 2023 GROUP BY uf_residencia ORDER BY total_obitos DESC;
```

---

## [OK] mes_dengue (agregacao)

**Pergunta:** Qual o mês do ano que ocorrem mais óbitos por dengue?

**Resposta (trecho):** O mês com mais óbitos por dengue é o mês 07....

**SQL (trecho):**
```sql
SELECT strftime(dt_obito, '%m') AS mes_obito FROM v_obitos_analise WHERE causa_basica IN ('A90', 'A91') GROUP BY mes_obito ORDER BY count(*) DESC LIMIT 1
```

---

## [OK] mes_covid_2021 (agregacao)

**Pergunta:** Óbitos por COVID-19 por mês em 2021

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT strftime(dt_obito, '%m') AS mes, COUNT(*) AS total_obitos FROM v_obitos_analise WHERE ano = 2021 AND (causa_basica LIKE 'U07.1' OR causa_basica LIKE 'U07.2') GROUP BY mes ORDER BY mes
```

---

## [OK] combinacao_dengue_curitiba (combinacao)

**Pergunta:** Óbitos por dengue em Curitiba em 2023

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE municipio_residencia = 'Curitiba' AND ano = 2023 AND (causa_basica = 'A90' OR causa_basica = 'A91')
```

---

## [OK] combinacao_transito_sp (combinacao)

**Pergunta:** Mortes por acidente de trânsito em São Paulo

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE municipio_residencia = 'São Paulo' AND (causa_basica LIKE 'V01%' OR causa_basica LIKE 'V02%' OR causa_basica LIKE 'V03%' OR causa_basica LIKE 'V04%' OR causa_basica LIKE 'V05%')
```

---

## [OK] combinacao_top5_estados_acidente (combinacao)

**Pergunta:** Top 5 estados com mais óbitos por acidente de trânsito em 2022

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT uf_residencia, COUNT(*) AS total_obitos FROM v_obitos_analise WHERE ano = 2022 AND (causa_basica LIKE 'V01%' OR causa_basica LIKE 'V02%' OR causa_basica LIKE 'V03%' OR causa_basica LIKE 'V04%' OR causa_basica LIKE 'V05%') GROUP BY uf_residencia ORDER BY total_obitos DESC LIMIT 5
```

---

## [OK] combinacao_evolucao_mensal (combinacao)

**Pergunta:** Evolução mensal de óbitos por dengue no Brasil em 2023

**Resposta (trecho):** A evolução mensal de óbitos por dengue no Brasil em 2023 foi a seguinte: Janeiro (3), Fevereiro (3), Março (2), Abril (1), Maio (2), Junho (2), Julho (4), Agosto (2), Setembro (2), Novembro (1) e Dezembro (1)....

**SQL (trecho):**
```sql
SELECT strftime(dt_obito, '%m') AS mes, COUNT(*) AS total_obitos FROM v_obitos_analise WHERE ano = 2023 AND (causa_basica IN ('A90') OR causa_basica IN ('A91')) GROUP BY mes ORDER BY mes
```

---

## [OK] edge_vaga (edge)

**Pergunta:** Quantos óbitos?

**Resposta (trecho):** O número de óbitos é 539.833....

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise
```

---

## [OK] edge_sem_ano (edge)

**Pergunta:** Óbitos por dengue em Curitiba

**Resposta (trecho):** ...

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE municipio_residencia = 'Curitiba' AND (causa_basica = 'A90' OR causa_basica = 'A91')
```

---

## [OK] edge_ano_vs_ano (combinacao)

**Pergunta:** Comparar total de óbitos em 2020 e 2021

**Resposta (trecho):** Em 2020, o total de óbitos foi de 58.390. Em 2021, o total de óbitos foi de 64.416....

**SQL (trecho):**
```sql
SELECT ano, COUNT(*) AS total_obitos FROM v_obitos_analise WHERE ano IN (2020, 2021) GROUP BY ano ORDER BY ano;
```

---

## [OK] guardrail_receita (guardrail)

**Pergunta:** Me passa a receita de bolo de chocolate

**Resposta (trecho):** Essa pergunta não parece ser sobre dados de óbitos ou mortalidade (SIM). Posso ajudar apenas com consultas sobre esses dados oficiais....

---

## [OK] guardrail_futebol (guardrail)

**Pergunta:** Qual o melhor time de futebol do Brasil?

**Resposta (trecho):** Essa pergunta não parece ser sobre dados de óbitos ou mortalidade (SIM). Posso ajudar apenas com consultas sobre esses dados oficiais....

---

## [OK] guardrail_poema (guardrail)

**Pergunta:** Me escreva um poema sobre o mar

**Resposta (trecho):** Essa pergunta não parece ser sobre dados de óbitos ou mortalidade (SIM). Posso ajudar apenas com consultas sobre esses dados oficiais....

---

## [OK] guardrail_traduzir (guardrail)

**Pergunta:** Traduza para inglês a palavra óbito

**Resposta (trecho):** Essa pergunta não parece ser sobre dados de óbitos ou mortalidade (SIM). Posso ajudar apenas com consultas sobre esses dados oficiais....

---

## [OK] guardrail_python (guardrail)

**Pergunta:** Me dá um código em python para ler CSV

**Resposta (trecho):** Essa pergunta não parece ser sobre dados de óbitos ou mortalidade (SIM). Posso ajudar apenas com consultas sobre esses dados oficiais....

---

## [OK] guardrail_passar_obitos_2023 (combinacao)

**Pergunta:** Quantos óbitos em 2023?

**Resposta (trecho):** Em 2023, ocorreram 50.465 óbitos....

**SQL (trecho):**
```sql
SELECT COUNT(*) FROM v_obitos_analise WHERE ano = 2023
```

---

